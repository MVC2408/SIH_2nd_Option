"""Synthetic data generator — NOT YET IMPLEMENTED.

Planned for Day 1, hours 4-6 (full-scale generator, 15-20 entities across
2-3 sectors, with seeded true anomalies, false-positive controls, and noise)
per docs/ARCHITECTURE.md. A minimal, hand-built version for the Go/No-Go
test (hours 1-3) may land here first as a separate, clearly-labelled script
rather than the full generator.

Must use src.config.RANDOM_SEED and no other seed, so the dataset —
including which entities/cases are anomalies or false-positive controls —
is reproducible.
"""
