"""Database access layer: connection management + canonical schema.

This package intentionally contains no data-generation or detection logic —
see src/generation/ and src/detection/ (both stubs on Day 1).
"""
